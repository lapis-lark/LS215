// will the array always contain only numbers? if not, what should be done with other elements?
// will there always be a single argument? if not, what should be done with the other arguemnts?
// how should invalid argument types be handled?

// PRIME NUMBERS
// should all number substrings be tested for primeness? or greedy numbers?
// should duplicates be printed?
// negative nums?

// FLATTEN UNIQUE
// which array is given priority?
// will i always have one arg?
// what to do with more args?
// can it contain NaN? 
// are array elements other than strs and nums allowed? how ot treat them?
 